/**
 * Træcera uses Tailwind CSS v4. Scan globs are declared in `./src/index.css`
 * (`@source` directives). PostCSS wires the compiler via `./postcss.config.mjs`.
 *
 * Keep this stub so editors / scripts that probe for “tailwind.config” still find one.
 */

export default {};
